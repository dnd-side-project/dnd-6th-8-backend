package com.travel.domain.day.repository;

import com.travel.domain.day.entity.DaysImage;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DayImageRepository extends JpaRepository<DaysImage, Long> {
}
